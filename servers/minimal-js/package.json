{
  "name": "foxwhisper-minimal-server",
  "version": "0.1.0",
  "description": "Minimal metadata-only FoxWhisper reference server (handshake + data routing)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "FOXW_CRYPTO_BACKEND=real vitest run",
    "test:watch": "FOXW_CRYPTO_BACKEND=real vitest",
    "demo": "FOXW_CRYPTO_BACKEND=real tsx demo/minimal-chat.ts",
    "lint": "echo 'lint skipped (no config)'",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cbor": "^10.0.11",
    "pqclean": "^0.8.1",
    "ws": "^8.17.0"
  },
  "devDependencies": {
    "@types/pqclean": "^0.3.2",
    "@types/ws": "^8.5.10",
    "tsx": "^4.21.0",
    "typescript": "^5.6.3",
    "vitest": "^4.0.15"
  }
}
